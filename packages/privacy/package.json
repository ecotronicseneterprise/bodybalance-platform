{
  "name": "@bodybalance/privacy",
  "version": "0.1.0",
  "private": true,
  "description": "PII firewall (BLUEPRINT 5.7): detectPII, sanitizeInput, buildClinicalContext, validateOutput. Platform-level — no clinic config can weaken it. Zero dependencies, reusable across AI products.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test src/index.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
