{
  "name": "@bodybalance/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Domain services + repositories: BookingService, AvailabilityService, PatientService, KnowledgeService (BLUEPRINT 4.2). The only layer that touches @bodybalance/database.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test src/availability.test.ts"
  },
  "dependencies": {
    "@bodybalance/database": "*",
    "@bodybalance/events": "*",
    "@bodybalance/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
