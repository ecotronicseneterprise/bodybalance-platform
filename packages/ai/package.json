{
  "name": "@bodybalance/ai",
  "version": "0.1.0",
  "private": true,
  "description": "LLM orchestration: prompts, RAG, guardrails, tool definitions (BLUEPRINT section 5). Calls @bodybalance/domain only — never @bodybalance/database (BLUEPRINT 4.2). Provider-abstracted from day one.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bodybalance/domain": "*",
    "@bodybalance/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
