/**
 * @bodybalance/ui — shared component library (BLUEPRINT 4.1).
 *
 * Brand values (colors, logo) always arrive as props/CSS variables resolved
 * from clinic_settings — components never hardcode a clinic's identity.
 * Populated from Sprint 2 (admin) and Sprint 3 (patient experience).
 */

export const UI_PACKAGE_READY = false as const;
